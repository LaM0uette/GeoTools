<UserControl x:Class="GeoTools.Views.DlgViewAll"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GeoTools.Views"
             mc:Ignorable="d">

    <Grid>
        <!-- <DataGrid Margin="0, 24, 0, 207" Name="dataGrid" ItemsSource="{Binding}"></DataGrid> -->
        <!-- -->
        <!-- <ItemsControl ItemsSource="{Binding MyCommands}"> -->
        <!--     <ItemsControl.Resources> -->
        <!--         <DataTemplate DataType="{x:Type local:MyCommandWrapper}"> -->
        <!--             <Button Content="{Binding DisplayName}" Command="{Binding command}"/> -->
        <!--         </DataTemplate> -->
        <!--     </ItemsControl.Resources> -->
        <!-- </ItemsControl> -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <WrapPanel x:Name="Panel" Width="800"/>
        </ScrollViewer>
    </Grid>
</UserControl>
